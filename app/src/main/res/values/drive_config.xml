<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      OAuth 2.0 "Web application" client ID created in Google Cloud Console
      (APIs & Services > Credentials), in the same project where the OAuth
      consent screen and the Android app are registered (SHA-1 of the
      signing certificate + applicationId com.marcogn.thepatientgamerhelper).

      Used by Credential Manager (GetGoogleIdOption.serverClientId) for the
      "Sign in with Google" authentication: it identifies the project the
      returned ID token was issued for. It is not the Android client ID
      (which needs no code-side configuration, it's tied to SHA-1+package
      automatically) — it must be the "Web application" type client ID.

      See CLAUDE.md, "Phase 4 — Google Drive cloud backup" section, for the
      rest of the external configuration (drive.appdata scope, testing mode
      of the consent screen).
    -->
    <string name="google_oauth_web_client_id" translatable="false">252469296963-954ul65p230dsbc7gfimo3s2d1519m1l.apps.googleusercontent.com</string>
</resources>
